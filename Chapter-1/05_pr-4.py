#4.Write a program to print the contents of a directory using OS module.Search online for the function which does that
#5.Label the program written in Prob 4 with comments.

import os       #Importing the module
print(os.listdir())  #Function to display all the directory content